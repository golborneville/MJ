<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nav_when">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".termSearch"></activity>
        <activity android:name=".personWebview" />
        <activity android:name=".ViewDiary" />
        <activity android:name=".LanguageConv" />
        <activity android:name=".DevelopersNote" />
        <activity android:name=".CopyRight" />
        <activity android:name=".settings" />
        <activity android:name=".dict_365_view" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".fragment.logInOut"
            android:label="@string/app_name" />
        <activity
            android:name=".logInOut_"
            android:label="@string/app_name" />
        <activity
            android:name=".Calendar_v"
            android:label="@string/app_name" />
        <activity
            android:name=".fragment.WriteDiary"
            android:label="@string/app_name" />
        <activity
            android:name=".FixDiary"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>